var React = require('react');

var StatusWindow = React.createClass({
	render: function() {
		return (
			<div className="statusWindow">
				this should contain the player status
			</div>
		)
	}
});

module.exports = StatusWindow;
